import 'package:flutter/material.dart';

// NovaCart brand colors — light mode (default theme)
class AppColors {
  // Backgrounds
  static const Color backgroundBase = Color(0xFFF4F8F6);
  static const Color glassPanelFill = Color(0xA6FFFFFF); // white at 65% opacity

  // Borders
  static const Color glassBorder = Color(
    0x140F2820,
  ); // dark green at 8% opacity

  // Accent colors
  static const Color emerald = Color(0xFF0FA06B);
  static const Color emeraldBright = Color(0xFF1FD18C);
  static const Color blue = Color(0xFF2C6FE0);
  static const Color amber = Color(0xFFD98A1A);

  // Text colors
  static const Color textHigh = Color(0xFF102822); // headlines
  static const Color textMedium = Color(0xFF5B6E67); // descriptions
  static const Color textLow = Color(0xFF8B9C96); // hints/captions
}
